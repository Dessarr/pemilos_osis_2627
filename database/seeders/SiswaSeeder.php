<?php

namespace Database\Seeders;

use App\Models\Murid;
use Illuminate\Database\Seeder;

class SiswaSeeder extends Seeder
{
    /**
     * Run the database seeds.
     * 
     * Note: Seeder ini sudah digabungkan ke DatabaseSeeder
     * Gunakan DatabaseSeeder untuk seeding data murid
     */
    public function run(): void
    {
        // Seeder ini sudah digabungkan ke DatabaseSeeder
        // Gunakan: php artisan db:seed atau php artisan migrate:fresh --seed
    }
}
